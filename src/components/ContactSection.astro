<section class="contact-section" id="contact">
  <div class="contact-container">

    <div class="contact-content">

      <span class="contact-label">CONTACT</span>

      <h2>
        Have a Question?
        <span>Get in Touch</span>
      </h2>

      <p class="contact-intro">
        Do you have a question about the ebook, the purchase process, or your
        download? Send me an email and I will reply as soon as possible.
      </p>

      <div class="contact-list">

        <div class="contact-item">
          <div class="contact-icon" aria-hidden="true">✉</div>

          <div>
            <span>Email</span>

            <a href="mailto:novis1928@gmail.com">
              novis1928@gmail.com
            </a>
          </div>
        </div>

        <div class="contact-item">
          <div class="contact-icon" aria-hidden="true">⏱</div>

          <div>
            <span>Response time</span>

            <p>Usually within 1–2 business days</p>
          </div>
        </div>

        <div class="contact-item">
          <div class="contact-icon" aria-hidden="true">🔒</div>

          <div>
            <span>Secure purchase</span>

            <p>Checkout and delivery are handled by Payhip and Gumroad</p>
          </div>
        </div>

      </div>

    </div>

    <div class="contact-card">

      <span class="contact-card-label">QUICK CONTACT</span>

      <h3>Send an Email</h3>

      <p>
        Click the button below to open your email application.
      </p>

      <a
        class="contact-button"
        href="mailto:novis1928@gmail.com?subject=Weight%20Loss%20Without%20Starving%20-%20Question"
      >
        Contact the Author
        <span>→</span>
      </a>

      <div class="contact-divider"></div>

      <div class="contact-help">
        <strong>Purchase question?</strong>

        <p>
          Include the email address used for your order so your purchase can be
          found more quickly.
        </p>
      </div>

    </div>

  </div>
</section>

<style>
  .contact-section {
    padding: 110px 0;
    background:
      radial-gradient(
        circle at top right,
        rgba(34, 197, 94, 0.12),
        transparent 34%
      ),
      #ffffff;
  }

  .contact-container {
    width: min(1180px, calc(100% - 40px));
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(320px, 0.9fr);
    align-items: center;
    gap: 72px;
  }

  .contact-content {
    max-width: 680px;
  }

  .contact-label {
    display: inline-block;
    margin-bottom: 18px;
    padding: 8px 14px;
    border-radius: 999px;
    background: rgba(34, 197, 94, 0.1);
    color: #16a34a;
    font-size: 13px;
    font-weight: 800;
    letter-spacing: 0.08em;
  }

  .contact-content h2 {
    margin: 0;
    color: #0f172a;
    font-size: clamp(38px, 5vw, 60px);
    line-height: 1.05;
    letter-spacing: -0.04em;
  }

  .contact-content h2 span {
    display: block;
    color: #22c55e;
  }

  .contact-intro {
    max-width: 620px;
    margin: 24px 0 0;
    color: #64748b;
    font-size: 18px;
    line-height: 1.8;
  }

  .contact-list {
    display: grid;
    gap: 18px;
    margin-top: 36px;
  }

  .contact-item {
    display: flex;
    align-items: center;
    gap: 18px;
    padding: 20px;
    border: 1px solid rgba(15, 23, 42, 0.07);
    border-radius: 18px;
    background: #ffffff;
    box-shadow: 0 16px 34px rgba(15, 23, 42, 0.05);
  }

  .contact-icon {
    display: grid;
    width: 46px;
    height: 46px;
    flex-shrink: 0;
    place-items: center;
    border-radius: 14px;
    background: #dcfce7;
    color: #16a34a;
    font-size: 20px;
    font-weight: 900;
  }

  .contact-item span {
    display: block;
    margin-bottom: 5px;
    color: #0f172a;
    font-size: 15px;
    font-weight: 800;
  }

  .contact-item a {
    color: #16a34a;
    font-weight: 700;
    text-decoration: none;
    word-break: break-word;
  }

  .contact-item a:hover {
    text-decoration: underline;
  }

  .contact-item p {
    margin: 0;
    color: #64748b;
    line-height: 1.6;
  }

  .contact-card {
    position: relative;
    padding: 44px 36px;
    overflow: hidden;
    border-radius: 28px;
    background: #0f172a;
    color: #ffffff;
    box-shadow: 0 30px 70px rgba(15, 23, 42, 0.2);
  }

  .contact-card::before {
    content: "";
    position: absolute;
    top: -90px;
    right: -90px;
    width: 240px;
    height: 240px;
    border-radius: 50%;
    background: rgba(34, 197, 94, 0.14);
  }

  .contact-card-label {
    position: relative;
    z-index: 1;
    display: inline-block;
    color: #4ade80;
    font-size: 12px;
    font-weight: 900;
    letter-spacing: 0.14em;
  }

  .contact-card h3 {
    position: relative;
    z-index: 1;
    margin: 12px 0 14px;
    font-size: 34px;
    letter-spacing: -0.03em;
  }

  .contact-card > p {
    position: relative;
    z-index: 1;
    margin: 0;
    color: #cbd5e1;
    font-size: 16px;
    line-height: 1.7;
  }

  .contact-button {
    position: relative;
    z-index: 1;
    display: inline-flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    min-height: 56px;
    margin-top: 30px;
    padding: 0 20px;
    border-radius: 14px;
    background: #22c55e;
    color: #ffffff;
    font-size: 16px;
    font-weight: 900;
    text-decoration: none;
    box-shadow: 0 16px 32px rgba(34, 197, 94, 0.26);
    transition:
      background 0.2s ease,
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }

  .contact-button:hover {
    background: #16a34a;
    transform: translateY(-2px);
    box-shadow: 0 20px 38px rgba(34, 197, 94, 0.34);
  }

  .contact-button span {
    font-size: 22px;
  }

  .contact-divider {
    position: relative;
    z-index: 1;
    height: 1px;
    margin: 30px 0;
    background: rgba(255, 255, 255, 0.12);
  }

  .contact-help {
    position: relative;
    z-index: 1;
  }

  .contact-help strong {
    display: block;
    margin-bottom: 8px;
    color: #ffffff;
  }

  .contact-help p {
    margin: 0;
    color: #94a3b8;
    line-height: 1.65;
  }

  @media (max-width: 940px) {
    .contact-container {
      grid-template-columns: 1fr;
      gap: 48px;
    }

    .contact-content {
      max-width: none;
    }

    .contact-card {
      max-width: 660px;
      width: 100%;
      margin: 0 auto;
    }
  }

  @media (max-width: 620px) {
    .contact-section {
      padding: 82px 0;
    }

    .contact-container {
      width: min(100% - 24px, 1180px);
    }

    .contact-intro {
      font-size: 16px;
    }

    .contact-item {
      align-items: flex-start;
      padding: 17px;
    }

    .contact-card {
      padding: 34px 24px;
      border-radius: 22px;
    }

    .contact-card h3 {
      font-size: 29px;
    }
  }
</style>